# password_strength_checker
This project is a simple Python script that checks the strength of user passwords. The user enters 5 passwords, and the program verifies if each password has at least 8 characters. If yes, it prints that the password is strong, otherwise it says the password is not strong enough.
